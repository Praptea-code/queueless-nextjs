# Tests

Placeholder for unit and API tests. Add Vitest or Jest + MSW once the core
queue logic is wired to a real database.

Suggested coverage:
- `tests/api/` — join, call-next, no-show route behavior
- `tests/components/` — QueueTicketCard, JoinQueueForm, LivePositionBadge